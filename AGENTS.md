# 项目说明：自动驾驶地图知识学习

## 核心目标

本项目用于系统学习自动驾驶所需的地图知识，并通过可运行的小型示例理解地图数据的结构、生成、读取、转换、校验和可视化。

后续处理本项目任务时，应优先围绕以下方向展开：

- 自动驾驶地图的基本概念：道路、车道、车道边界、中心线、连接关系、路口、停止线、交通标志、信号灯和限速等。
- 地图中的坐标系与定位基础：WGS84、经纬度、局部 ENU、投影坐标，以及不同坐标系之间的转换。
- 路由和拓扑关系：前驱、后继、相邻车道、变道关系和道路连通性。
- 常见地图格式及其适用场景，重点包括 Lanelet2（OSM/XML）和 OpenDRIVE（XODR）；需要轻量演示时也可以使用 GeoJSON 或设计清晰的简化教学格式。
- 地图数据的解析、生成、转换、验证和可视化示例。

## 默认工作方式

当用户提出地图相关的学习或开发需求时：

1. 先用简明语言解释涉及的地图概念，再提供最小可运行示例。
2. 可以按任务需要创建简单的“智驾地图”数据文件、示例代码、测试和说明文档，无需反复确认低风险细节。
3. 如果用户没有指定格式，根据学习目标选择：
   - 学习车道级语义与规则：优先使用 Lanelet2。
   - 学习道路几何、车道截面和仿真地图：优先使用 OpenDRIVE。
   - 学习基础几何、坐标和可视化：可以使用 GeoJSON。
   - 仅用于讲清数据结构：可以使用自定义 JSON，但必须明确说明它不是行业标准。
4. 示例地图应尽量小而完整，例如一段直路、双向道路、十字路口或简单变道场景。
5. 示例代码应包含必要注释、运行方法、输入输出说明，并优先保证容易阅读和复现。
6. 涉及坐标时必须明确坐标系、单位、原点和轴方向；涉及地图格式时必须区分标准字段与教学简化字段。
7. 不把教学示例描述为可直接用于量产车辆；应指出相对真实高精地图缺少的内容和限制。

## 建议的项目结构

随着内容增加，优先采用以下结构；不要为了空目录预先创建无内容的文件夹：

```text
docs/          # 地图知识笔记与格式说明
maps/          # 示例地图数据，按格式或场景分类
examples/      # 地图生成、解析、转换和可视化代码
tests/         # 数据与代码校验
tools/         # 可复用的小型命令行工具
```

## 示例与验证约定

- 优先使用 Python 编写教学示例，除非用户指定其他语言或项目后续确立了不同技术栈。
- 新增地图数据时，同时提供字段说明或对应的读取示例。
- 用户当前偏好自行验证学习示例。新增 OpenDRIVE 数据、生成代码或转换代码后，默认直接交付，不主动运行解析器、查看器、测试或其他验证命令；只有用户明确要求验证，或用户反馈具体问题时才执行相应检查。
- 浮点坐标比较使用合理容差，不依赖完全相等。
- 文件名和目录名使用清晰、稳定的英文；面向学习者的说明默认使用中文。
- 测试代码中的断言说明、失败提示和面向学习者的控制台输出默认使用中文；测试函数名、变量名、类型名等代码标识符继续使用英文。除非是在验证协议要求的固定英文文本，否则不要把英文说明字符串直接展示给学习者。
- 引入第三方依赖前，优先选择成熟、轻量且与目标格式直接相关的库，并记录安装与版本要求。

## 当前用户意图

用户的核心目的不是开发完整的自动驾驶系统，而是学习自动驾驶需要用到的地图知识。后续可根据具体问题逐步创建简单的智驾地图数据和配套示例代码，形成可持续扩展的学习项目。

## 当前学习进度

- 自动驾驶地图整体认知：已完成基础学习。
- Lane Model：已基本了解。
- OpenDRIVE：当前学习阶段，入门主线约完成 65%。
  - 已掌握基础 XML 结构和 `.xodr` 文本格式。
  - 已掌握参考线几何中的 `s`、`x`、`y`、`hdg`、`length`、`line` 和 `arc`，以及曲率、半径、圆弧长度、位置和方向连续性。
  - 已掌握 `laneSection`、`center/left/right`、车道正负编号、`lane 0`、`width`、`roadMark` 和 `RHT/LHT`。
  - 已掌握普通道路级 `predecessor/successor`、`elementType`、`elementId`、`contactPoint`，以及车道级前驱后继关系。
  - 已理解拓扑正确不等于几何连续，地图质检还需检查端点、航向、曲率、宽度、边界和拓扑几何一致性。
  - 已理解 Junction 的基本目的和职责：入口 Road 连接 Junction，Junction 管理多分支通行关系，Connecting Road 表达路口内实际转向几何，`laneLink` 表达入口车道到连接车道的映射。
  - 当前正在学习 Junction 中的 `connection` 和 `laneLink`，重点是 `incomingRoad`、`connectingRoad`、`contactPoint`、`from/to`、多 laneLink 以及多入口多出口组织方式。
  - 尚未系统学习复杂 Junction、`spiral` 缓和曲线及 C++ OpenDRIVE 解析与验证。
- Lanelet2、Apollo HD Map、Mini HD Map 实现：尚未正式开始。
