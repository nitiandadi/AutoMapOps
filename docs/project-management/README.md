# AutoMapOps 项目管理总览

本文档用于回答两个问题：

1. 整个练手项目需要完成哪些阶段；
2. 当前真实做到哪一步，下一步应该做什么。

详细任务、依赖关系和验收条件见 [tasks.md](./tasks.md)。

## 项目目标

使用 C++ 为主要开发语言，实现一个面向物流园区、矿区等封闭场景的自动驾驶地图生产与车端交付平台，跑通以下闭环：

```text
首版地图草稿 V0
→ 地图编辑与拓扑构建
→ 规则质检
→ 正式 MapVersion V1
→ C++ 地图编译
→ MapPackage V1
→ C++ 车端 SDK 加载、查询与路径规划
→ Candidate Map Patch
→ MapVersion V2
→ 动态事件覆盖与重新规划
```

## 当前状态

更新时间：2026-09-01

当前阶段：**M3 Canonical IO、质检与 MapVersion V1（进行中）**

当前结论：

- 项目定位已经确定；
- 云端和车端职责已经确定；
- C++ 已确定为核心开发语言；
- V0、MapVersion、MapPackage、Candidate Patch、Dynamic Event 的概念边界已经讨论清楚；
- 五类对象的正式设计已经写入 [核心数据契约](../architecture/core-data-contracts.md)；
- [坐标约定](../architecture/coordinate-system.md)和[第一版 MVP 范围](../architecture/mvp-scope.md)已经冻结；
- 根 CMake 工程已经建立，并已使用 MSVC 和 C++20 完成配置验证；
- `map_core` 静态库已经建立，并已成功生成 `automap_map_core.lib`；
- `map_io` 静态库已经建立，显式依赖 `map_core`，并为 Canonical JSON 读写预留了公共接口；
- `map_validation` 已提供问题等级、质检报告和规则抽象接口；
- `map_version` 已提供版本 ID、发布结果与发布资格判断接口；
- `automap_cli` 已建立并可输出程序名称、版本和 Canonical 格式名称；
- CTest测试工程已经建立，M1冒烟测试覆盖核心模块联通和发布阻断逻辑；
- 配置、编译、测试、CLI运行和Visual Studio打开方式已经形成构建说明；
- 已完成 M2 Canonical Map Model 分层、所有权、引用和模块职责设计；
- 已实现基础几何类型、算法及模型骨架测试；
- 已实现 Road、Lane 和独立 LaneBoundary 模型，明确车道拓扑、共享边界、几何方向及跨越权限；
- 已实现 Junction、场景对象和车辆约束，并由 `MapData` 统一持有、查询和比较全部对象；
- 已完成物流园 V0 道路与车道拓扑：7 条 Road、12 条 Lane、1 个显式 Junction，形成主路、装卸绕行、汇流和回程闭环；
- 已生成 `maps/drafts/logistics_park_v0.json` 及中文坐标、对象和拓扑说明；
- 已完成 Canonical JSON 字符串/文件读取与写出，物流园 V0 可完整转换为 `MapData`，并可稳定写出后读回；
- 已实现 `inspect` 命令，可查看地图标识、坐标参考、ENU 范围和九类对象统计；
- 已实现九类地图对象的全局 ID 唯一性规则，重复 ID 会产生 Fatal 并阻止发布；
- 已实现引用完整性规则，覆盖当前模型全部显式 ID 引用，悬空引用会产生 Error 并阻止发布；
- 已实现基础几何规则，覆盖折线、区域轮廓、车道宽度、WGS84 原点和局部 ENU 坐标范围；
- 已实现拓扑互反规则，覆盖 Road/Lane 前驱后继及 Road-Lane、Junction-LaneConnection 双向所有权；
- 已实现连接几何规则，按实际行驶方向检查 Road、Lane 和显式 LaneConnection 的端点距离与 XY 航向差；
- 已实现路网可达性规则，验证 Warehouse 场景中 Gate 到每个 LoadingBay 至少存在一条符合开放状态、宽度和限行白名单的车辆路径；
- 测试源码已按 M1、M2、M3 里程碑分目录组织，CTest 名称保持相同阶段前缀；
- 尚未产生 MapVersion V1 或 MapPackage V1。

## 进度看板

| 里程碑 | 目标 | 状态 | 已完成/总任务 | 主要产物 |
|---|---|---|---:|---|
| M0 | 项目定义与技术基线 | 已完成 | 8/8 | 数据契约、坐标约定、MVP 边界 |
| M1 | C++ 最小工程架子 | 已完成 | 8/8 | 可编译、可测试的 C++20 工程 |
| M2 | Canonical 模型与物流园 V0 | 已完成 | 10/10 | `logistics_park_v0.json` |
| M3 | 质检与 MapVersion V1 | 进行中 | 9/12 | `MapVersion V1` 目录与质检报告 |
| M4 | 地图编译与 MapPackage V1 | 未开始 | 0/11 | `manifest.json`、`map.bin`、空间索引 |
| M5 | C++ Vehicle Map SDK | 未开始 | 0/10 | 加载、查询、匹配和路径规划 |
| M6 | 地图生产 Web 平台 | 未开始 | 0/12 | Web 工作台与 C++ 平台服务 |
| M7 | Candidate Patch 与 V2 | 未开始 | 0/10 | Patch 审核、ChangeSet、V2 闭环 |
| M8 | Dynamic Event 闭环 | 未开始 | 0/7 | 临时封路、限速和车端重规划 |
| M9 | 矿区扩展与作品包装 | 未开始 | 0/8 | Mining Profile、演示与项目文档 |

> 进度只按已经落入仓库并通过验收的成果计算。讨论完成但尚未形成文档或代码的内容，不计为实现完成。

## 当前下一步

按顺序只推进下面三项：

1. `M3-10`：生成质检报告；
2. `M3-11`：实现版本发布；
3. `M3-12`：验证 MapVersion V1。

M0、M1 和 M2 已完成，M3-01 至 M3-09 已完成，现在进入 `M3-10`。

## 里程碑依赖关系

```text
M0 项目定义
 ↓
M1 C++ 工程架子
 ↓
M2 Canonical 模型 + 物流园 V0
 ↓
M3 质检 + MapVersion V1
 ↓
M4 编译器 + MapPackage V1
 ↓
M5 Vehicle Map SDK
 ↓
M6 Web 地图生产平台
 ↓
M7 Candidate Patch + V2
 ↓
M8 Dynamic Event
 ↓
M9 矿区扩展与作品包装
```

M4 和 M5 完成后，首版地图生产与车端消费的最小纵向闭环才算完成。M7 完成后，地图持续更新闭环才算完成。

## 状态定义

任务只使用以下四种状态：

| 标记 | 含义 |
|---|---|
| `✅` | 已完成，并满足任务验收条件 |
| `🟡` | 正在实施，但尚未满足全部验收条件 |
| `⬜` | 尚未开始 |
| `⛔` | 被明确问题阻塞，需要先解决依赖 |

## 更新规则

每完成一个任务，应同步进行以下操作：

1. 在 [tasks.md](./tasks.md) 中更新任务状态；
2. 更新本页里程碑的“已完成/总任务”；
3. 把“当前下一步”移动到下一项未完成任务；
4. 在下面的进展记录中增加一条记录；
5. 如果设计发生变化，在记录中说明原因和影响范围。

## 进展记录

| 日期 | 变更 | 结果 |
|---|---|---|
| 2026-08-06 | 建立项目管理总览和任务清单 | 项目进入 M0，代码开发尚未开始 |
| 2026-08-06 | 完成五类核心数据契约设计 | M0 完成 6/8，下一任务为 M0-07 |
| 2026-08-06 | 完成坐标约定和 MVP 范围 | M0 完成 8/8，下一任务为 M1-01 |
| 2026-08-06 | 建立根 CMake 工程并完成 MSVC 配置验证 | M1 完成 1/8，下一任务为 M1-02 |
| 2026-08-07 | 建立 `map_core` 静态库并完成编译验证 | M1 完成 2/8，下一任务为 M1-03 |
| 2026-08-11 | 建立 `map_io` 静态库和 Canonical JSON 预留接口 | M1 完成 3/8，下一任务为 M1-04 |
| 2026-08-12 | 建立质检、版本模块和可运行 CLI | M1 完成 6/8，下一任务为 M1-07 |
| 2026-08-13 | 建立 CTest 冒烟测试并完成构建说明 | M1 完成 8/8，下一任务为 M2-01 |
| 2026-08-13 | 冻结 Canonical 模型架构，建立 M2 模型骨架并完成基础几何 | M2 完成 1/10，全部 2 项测试通过，下一任务为 M2-02 |
| 2026-08-15 | 完成 `MapHeader`、坐标参考默认值、值比较、字段说明和专门测试 | M2 完成 2/10，全部 3 项测试通过，下一任务为 M2-03 |
| 2026-08-15 | 完成 Road 参考线、Lane 侧别/方向/状态/属性/拓扑模型及说明 | M2 完成 3/10，干净重编译后全部 4 项测试通过，下一任务为 M2-04 |
| 2026-08-15 | 完成 LaneBoundary 类型、几何方向、共享关系、跨越权限模型及说明 | M2 完成 4/10，干净重编译后全部 5 项测试通过，下一任务为 M2-05 |
| 2026-08-15 | 完成 Junction、场景对象、车辆约束和 MapData 聚合根 | M2 完成 8/10，干净重编译后全部 9 项测试通过，下一任务为 M2-09 |
| 2026-08-27 | 完成物流园道路拓扑与 Canonical V0 草稿 | M2 完成 10/10，已生成 JSON 和中文设计说明，下一任务为 M3-01 |
| 2026-08-30 | 完成 Canonical JSON 读取与写出 | M3 完成 2/12，V0 全字段读入及整图读写往返测试通过，下一任务为 M3-03 |
| 2026-08-30 | 完成地图摘要命令与 ID 唯一性规则 | M3 完成 4/12，物流园摘要输出正确，跨九类对象的重复 ID 产生 Fatal，下一任务为 M3-05 |
| 2026-08-31 | 完成引用完整性规则并整理测试目录 | M3 完成 5/12，当前模型全部显式 ID 引用纳入 Error 检查，测试按 M1/M2/M3 分组，下一任务为 M3-06 |
| 2026-08-31 | 完成基础几何规则 | M3 完成 6/12，物流园 V0 通过检查，点数、长度、自相交、轮廓、宽度和坐标范围均有测试覆盖，下一任务为 M3-07 |
| 2026-08-31 | 完成拓扑互反规则 | M3 完成 7/12，Road/Lane 前驱后继与两组所有权双向关系纳入 Error 检查，下一任务为 M3-08 |
| 2026-08-31 | 完成连接几何规则 | M3 完成 8/12，Road、Lane 和显式连接按行驶方向检查 0.5 m 端点距离及 30° 航向差，下一任务为 M3-09 |
| 2026-09-01 | 完成路网可达性规则 | M3 完成 9/12，配送厢式车可从门岗到达 A1 月台，关闭 Lane、车辆过宽或不满足白名单时均能阻断，下一任务为 M3-10 |
